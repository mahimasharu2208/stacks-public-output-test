# deployments.tfdeploy.hcl

deployment "development" {
  # This inputs block is where you provide values for any variables
  # defined in your stack. Since our test has no variables, we leave it empty.
  inputs = {}
}
